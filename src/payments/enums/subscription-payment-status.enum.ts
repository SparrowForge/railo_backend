export enum SubscriptionPaymentStatus {
  created = 'created',
  initiated = 'initiated',
  pending = 'pending',
  paid = 'paid',
  failed = 'failed',
  canceled = 'canceled',
  expired = 'expired',
  refunded = 'refunded',
}
